{
  "name": "@lemmon/custom-textarea",
  "version": "0.0.3",
  "main": "index.js",
  "description": "Web Components custom-textarea element.",
  "author": "lemmon",
  "license": "MIT",
  "scripts": {
    "build": "browserify element.js | babel-env | minify > dist/element.js",
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags && npm publish",
    "w": "watchify element.js -o dist/element.js --verbose"
  },
  "browserslist": "firefox 63, chrome 67",
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 2018
    }
  }
}
